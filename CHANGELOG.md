# 更新日志

## 0.4.0 · 2026-09-23

本次更新新增单条回复与对话费用估算，并支持 TauriTavern 原生运行。

### 费用与用量统计

- **单条回复费用**：在角色头像下方显示当前回复的估算金额，点击即可查看输入、输出、推理 Token、缓存命中率、首内容耗时、总耗时及生成速率。
- **回复版本与续写**：滑动生成的各个版本分别记录费用，切换版本时同步显示；续写的用量与费用累计到当前版本，刷新页面后仍可查看。
- **对话费用汇总**：从“魔法棒 → 对话费用估算”查看当前对话的费用小计、最近一次费用、请求明细与缓存命中率。汇总命中率按缓存 Token 总数与输入 Token 总数计算。
- **价格管理**：支持自动匹配公开价格、手动覆盖单价及长上下文计价。模型名单与价格可在线更新，并保留离线缓存；已采集但缺少价格的记录可在补充单价后估算。

### TauriTavern 支持

- 自动识别 TauriTavern，在扩展设置中提供服务层级、PayGo-only 和区域配置，并支持为 Agent Model Target 单独设置层级。
- 复用宿主的认证、附加参数与本地存储，无需安装配套 Node 后端；普通聊天支持流式与非流式用量采集。
- 适配折叠设置面板、消息虚拟化和回复版本切换，消息滚动重新显示后可恢复费用入口。

### 界面与本地化

- 费用详情卡采用分区布局，支持手机、文档模式和键盘操作；生成中、待计价及部分估算状态分别显示。
- 提供可独立编辑的英文、简体中文和繁体中文语言文件，单条费用卡片跟随宿主语言，覆盖详情、提示和无障碍标签。
- 按请求关联对话与回复版本，避免切换聊天、并发请求或延迟返回的用量记入其他消息；后台与 Quiet 请求不会附加到角色回复上。

### 升级说明

- **SillyTavern / Luker**：前后端通信升级为协议 v2，请将前端扩展与配套服务端插件一并更新至 **0.4.0 或兼容版本**，随后重启宿主并刷新页面。Gemini Standard 请求也通过后端采集用量，不改变所选服务层级。
- **TauriTavern**：需使用提供原生附加参数与扩展存储接口的 **2.3.0 或更新兼容版本**，仅安装前端扩展即可。禁用或卸载扩展不会清除已保存的层级参数；如需恢复默认，请先选择 Standard 并关闭 PayGo-only。

### 统计范围与限制

- 金额为依据已采集用量和单价计算的本地估算，并非服务商结算账单；查询统计不会再次调用模型。旧消息未采集的用量无法补回，缺失数据不会作为完整费用展示。
- TauriTavern 非流式响应可能缺少推理或缓存等用量信息，相关费用标为“部分估算”，无法确认的指标显示 `—`；原生 Agent / 子 Agent 模型循环暂不纳入费用统计。
- 耗时与生成速率均为客户端观测值，受网络和响应缓冲影响，不代表服务端实际解码速度。

## 2026-09-16 · 请求与保存修复

- 修复 Connection Manager 和独立生成入口遗漏 PayGo 层级的问题；请求按自身 profile/preset 选择层级。
- 将预检延后至最终请求发送前，保留单次 `secret_id` 覆盖，避免使用错误密钥。
- 修复预设保存失败仍返回成功的问题，同时保证后续保存继续执行。
- 增加并发 profile、反代冲突、请求取消及失败拦截回归测试。

需同步更新服务端插件、重启酒馆并刷新页面。AI Studio 支持严格按 ID 选取密钥；Vertex Express/Full 的显式密钥 ID 暂时会明确报错拦截。
