# 更新日志

## 0.4.0 · 对话费用估算

- Gemini Standard 也经过后端代理，保留认证、区域与 PayGo-only 选择。前后端协议升级为 v2，需要一起更新。
- 新增魔法棒菜单“对话费用估算”，展示当前对话请求的实际用量和本地 USD 费用小计。
- 支持按来源、模型和层级配置价格、长上下文档位、请求价格快照及历史缺价记录补估。
- 支持名单与公开文本价格共用在线更新和离线缓存；手动价格优先，可在费用页恢复自动价格或立即更新。校验失败保留原数据，限时价格到期停止自动使用。
- 按请求发起时的对话关联用量，跨聊天、分支和并发隔离；重命名保留关联。
- 缺失用量、中断及不支持的计价情况明确标注，费用记录不随删除消息或诊断日志清空而删除。

## 2026-09-16 · 请求与保存修复

- 修复 Connection Manager 和独立生成入口遗漏 PayGo 层级的问题；请求按自身 profile/preset 选择层级。
- 将预检延后至最终请求发送前，保留单次 `secret_id` 覆盖，避免使用错误密钥。
- 修复预设保存失败仍返回成功的问题，同时保证后续保存继续执行。
- 增加并发 profile、反代冲突、请求取消及失败拦截回归测试。

需同步更新服务端插件、重启酒馆并刷新页面。AI Studio 支持严格按 ID 选取密钥；Vertex Express/Full 的显式密钥 ID 暂时会明确报错拦截。
